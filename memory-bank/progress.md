# Prompt2PRD Progress

> 当前阶段：阶段一已完成；阶段二的前端领域类型、IndexedDB Schema v1、项目 Repository、真实项目首页、文字新建项目流程、文件上传、预览、隐私确认和文本分块、项目工作台导航以及事务保存与版本记录已完成，阶段二完成，下一步进入阶段三（后端模型、错误与额度基础）。

## 已完成

- 产品设计文档、技术栈和 57 步实施计划已经建立。
- 已确认状态归属、完整度算法、文件分块、模型地址安全、系统 Key 默认策略、SSE 章节事件和浏览器范围。
- `memory-bank` 路径已经统一，架构与进度文档已经初始化。
- Git 仓库已经重新初始化。
- 已建立根 Maven 聚合工程与 `backend` 模块，锁定 Java 21、Spring Boot 4.1.0、Spring AI BOM 2.0.0 和 Maven Wrapper 3.9.11。
- 已创建 Spring Boot WebFlux 应用入口和应用上下文冒烟测试。
- 已建立独立 `frontend` 工程，配置 Vue 3、Vite、严格 TypeScript、Vue Router、Pinia、Vitest 和 Element Plus 按需导入。
- 已安装并通过 `package-lock.json` 精确锁定 Dexie、markdown-it、Mermaid 及全部步骤 3 构建和测试依赖。
- 步骤 3 创建的最小根组件、浏览器挂载入口和 Router/Pinia 初始化冒烟测试，已在步骤 5 演进为路由驱动的工作台外壳。
- 已建立统一生产构建：根 Maven 使用隔离的 Node/npm 执行 `npm ci` 与 Vite 构建，后端将产物复制到静态资源目录并生成包含前端的可运行 JAR。
- 已建立浅色桌面工作台外壳、根路由、空项目首页和八个设计文档颜色 Token；全局导航、主操作、空状态与本地数据提示均已呈现。
- 已补充 Prompt2PRD SVG favicon，浏览器页面加载时无控制台错误。
- 已定义项目、需求、问题、答案、冲突、版本、字段变更、完整度、架构候选和 PRD 章节领域类型，并用工厂函数阻止非法 UUID、非 UTC 时间、非法状态和非确认项锁定。
- 已建立 Dexie IndexedDB Schema v1，包含设计文档规定的 9 个对象仓库及项目隔离复合索引；测试环境使用精确锁定的 `fake-indexeddb` 6.2.5。
- 已实现项目 Repository 的创建、读取、列表、重命名、完整复制、归档、移入回收站、恢复和单项目永久删除；所有跨仓库复制与删除操作使用 Dexie 事务。
- 已实现真实项目首页、项目卡片和活动/归档/回收站视图；卡片展示阶段、完整度、待确认数和更新时间，操作集中在菜单中。
- 已实现新建项目页面和创建表单：首页两个创建入口已接通，文字输入按 Unicode 字符校验至少 5 字，临时名称取规范化输入前 20 字，创建成功后进入项目 UUID 路由并从 IndexedDB 重读项目。
- 已为后续首次分析命名建立 Repository 边界：空模型名称不覆盖临时名称，用户手动重命名后迟到的模型建议不会覆盖用户名称。
- 已实现 Markdown/TXT 文件上传：仅接受有效 UTF-8 文本，拒绝错误扩展名、空文件、无法解码文件和超过 2 MiB 的文件，超限内容不会被截断后继续处理。
- 已实现安全只读文本预览、文件字符数和片段数展示；脚本样式 Markdown 只作为 textarea 文本呈现，不进入 HTML 渲染。
- 已实现首次第三方模型数据传输提示，确认状态以 `uploadPrivacyNoticeAccepted` 写入现有 `app_setting`；后续上传不重复弹窗，但上传区持续展示简短隐私提醒。
- 已实现按 Markdown 标题和自然段优先分块、超长块按 Unicode 码点硬拆的确定性算法；每片不超过 32,000 字符，片段连接后保持解析全文完整。
- 已实现逐片顺序处理、进度和取消边界；本步骤默认只准备本地片段，尚未调用真实模型。确认后的完整文件可与补充文字共同进入现有项目创建流程，移除文件后恢复至少 5 字的文字校验。
- 已建立项目工作台导航：`ProjectHeader` 顶部栏展示项目名称、阶段标签、完整度百分比、当前模型、保存状态和"生成 PRD"主操作按钮。
- 已建立 `ProjectNavigation` 二级导航，包含需求概览、问题向导、需求卡片、架构建议、流程图和 PRD 六个模块入口，高亮当前活跃模块。
- 已建立 `ProjectWorkspace` 三栏工作台布局：二级导航（左）| 主画布 `<router-view>`（中）| 可收起辅助面板（右）。流程图和 PRD 页面右侧面板默认收起，符合 BR-038。
- 已更新路由为嵌套结构：`/projects/:projectId` 包含 6 个子路由，默认重定向至 `overview`；新建项目页导航目标已更新为 `project-overview`。
- 已新增 `PROJECT_MODULES` 常量和 `ProjectModule` 类型；`ProjectModulePlaceholder` 为各模块提供占位视图，后续步骤将替换为真实实现。

## 当前验证

- `2026-07-17` 运行 `.\mvnw.cmd test` 成功：根工程与后端模块均为 `SUCCESS`，测试 1 个、失败 0、错误 0、跳过 0。
- Spring 上下文使用 Java 21.0.6 和 Spring Boot 4.1.0 启动，Actuator 暴露默认健康端点。
- 依赖树确认 WebFlux 使用 Reactor Netty，且不存在 JDBC、JPA、R2DBC 或数据库驱动；当前仍没有服务端业务数据库。
- `2026-07-17` 运行 `npm --prefix frontend run test` 成功：测试文件 1 个、测试 1 个，全部通过。
- `2026-07-17` 运行 `npm --prefix frontend run build` 成功：`vue-tsc` 与 `tsc` 严格类型检查通过，Vite 生成 `frontend/dist` 生产产物。
- 首次构建发现 TypeScript 7.0.2 与 `vue-tsc` 3.3.7 的包导出不兼容，已固定到可通过类型检查的 TypeScript 6.0.3。
- `2026-07-17` 运行 `.\mvnw.cmd package` 成功：根工程与后端模块均为 `SUCCESS`，前端锁定依赖安装、类型检查、Vite 构建、后端测试、资源复制和 Spring Boot 重打包全部通过。
- JAR 内容检查确认当前静态资源为 `index.html`、`favicon.svg`、`index-6amIZEbi.js` 和 `index-CQfpKQaX.css`，没有旧哈希资源残留。
- 启动 `backend/target/backend-0.0.1-SNAPSHOT.jar` 后，根路径返回 HTTP 200 和打包后的 Prompt2PRD 页面，`/actuator/health` 返回 HTTP 200 与 `UP`；两者使用同一服务地址且响应没有 `Access-Control-Allow-Origin`。
- 当前尚无业务接口、模型接入和真实项目分析工作台；文件上传、预览与本地分块已经接入新建项目页，但项目入口仍只展示已保存状态，不声称已经启动 AI 分析。
- `2026-07-17` 运行 `npm --prefix frontend run test` 成功：测试文件 2 个、测试 4 个，全部通过；覆盖 Router/Pinia、四个全局导航入口、设计 Token、主按钮颜色契约、空状态和无深色模式。
- `2026-07-17` 运行 Playwright 真实浏览器检查成功：1280×800 下页面宽度为 1280、无横向溢出，侧栏、空状态和本地数据提示均可见；主按钮计算后背景为 `rgb(199, 235, 100)`、文字为 `rgb(38, 43, 37)`，控制台错误和警告均为 0。
- 视觉验收截图保存在 `output/playwright/step-5-workspace-1280.png`。
- `2026-07-17` 阶段一最终运行 `.\mvnw.cmd package` 成功：前端类型检查和构建、后端测试、资源复制与可执行 JAR 重打包全部通过，新页面的 HTML、CSS、JavaScript 和 favicon 共 4 个资源进入 JAR。
- 活动/归档/回收站导航、项目卡片操作以及“新建项目”“创建第一个项目”两个入口均已接通，模型设置仍禁用。
- `2026-07-17` 运行领域工厂和 IndexedDB 测试成功：首次以缺少模块得到预期失败，完成实现后完整前端测试为 5 个测试文件、12 项测试全部通过。
- IndexedDB 测试覆盖 9 个仓库、数据库版本 1、必要复合索引、项目写入后关闭重开仍存在，以及 Schema 中不存在 API Key、Authorization、凭据、密钥或 Token 字段。
- `2026-07-17` 运行 `npm --prefix frontend run typecheck` 和 `npm --prefix frontend run build` 成功；当时数据层尚未由项目首页调用，项目操作和跨仓库事务随后已由项目 Repository 实现。
- `2026-07-17` 数据层完成后运行 `.\mvnw.cmd package` 成功：`npm ci` 按锁文件安装 357 个包，前端类型检查与生产构建、后端 1 个测试及可执行 JAR 重打包全部通过。
- `2026-07-17` 项目 Repository 先以缺少模块得到预期失败，完成实现后专项测试 4 项通过；完整前端测试为 6 个测试文件、16 项测试全部通过，独立 TypeScript 检查与 Vite 生产构建成功。
- Repository 测试确认刷新式关闭重开保留数据、活动/归档/回收站列表隔离、副本内部引用全部使用新 ID、副本修改不影响原项目、普通删除可恢复、永久删除拒绝非回收站项目且只清理目标项目。
- `2026-07-17` 项目 Repository 完成后运行 `.\mvnw.cmd package` 成功：前端 `npm ci`、严格类型检查、生产构建、后端 1 个测试、4 个静态资源复制及 Spring Boot 可执行 JAR 重打包全部通过。
- `2026-07-17` 项目首页完成后运行完整前端测试成功：8 个测试文件、25 项测试全部通过；独立 TypeScript 严格检查和 Vite 生产构建成功，生产构建转换 45 个模块。
- 首页组件测试覆盖三类空状态、真实摘要筛选、项目元数据、重命名、复制、归档、移入回收站、恢复、永久删除确认、读取失败和操作失败保留当前列表；源码不包含宠物寄养等示例快捷内容。
- `2026-07-17` Playwright 真实浏览器验收成功：临时项目显示 68% 完整度与 2 项待确认，归档、回收站和恢复路径均成功；1280×800 下页面宽度为 1280、无横向溢出、唯一当前导航项，控制台错误和警告均为 0。
- 项目首页视觉验收截图保存在 `output/playwright/step-9-project-list-1280.png`；验收后只删除了本次创建的明确临时项目，未保留测试项目数据。
- `2026-07-17` 步骤 9 最终运行 `.\mvnw.cmd package` 成功：`npm ci` 安装 357 个锁定依赖，前端严格类型检查与 Vite 生产构建、后端 1 个测试、4 个静态资源复制及 Spring Boot 可执行 JAR 重打包全部通过；JAR 仅包含本次哈希资源。
- `2026-07-17` 步骤 10 完整前端测试为 10 个测试文件、42 项测试全部通过；覆盖短文字拒绝、有效文字创建、已确认文件内容绕过文字最短限制、20 字临时名称、创建失败不跳转、重复 ID 创建失败不增加项目，以及模型建议名称的三种保护场景。
- `2026-07-17` 步骤 10 独立 TypeScript 严格检查和 Vite 生产构建成功，生产构建转换 54 个模块；Playwright 在 1280×800 下验证首页入口、短输入错误、真实 IndexedDB 创建、刷新恢复和首页卡片，无横向溢出，控制台错误与警告均为 0。
- 新建项目视觉验收截图保存在 `output/playwright/step-10-new-project-1280.png`；验收后通过回收站流程永久删除了本次唯一的临时项目。
- `2026-07-17` 步骤 10 最终运行 `.\mvnw.cmd package` 成功：`npm ci` 安装 357 个锁定依赖，前端严格类型检查与生产构建、后端 1 个测试、4 个静态资源复制及 Spring Boot 可执行 JAR 重打包全部通过；JAR 只包含本次哈希资源。
- `2026-07-17` 步骤 11 按测试先行完成：解析器先以缺少模块得到预期失败，上传组件也先以缺少组件得到预期失败；完成实现后专项测试为 4 个测试文件、29 项全部通过。
- `2026-07-17` 步骤 11 完整前端测试成功：12 个测试文件、66 项测试全部通过；覆盖有效文件、BOM/换行、2 MiB 精确边界与超限、空文件、扩展名、无效 UTF-8、Unicode 多片边界、全文不丢失、顺序处理、中途取消、隐私状态持久化、重复提示保护、只读安全预览、文件创建、替换和清除后校验恢复。
- `2026-07-17` 步骤 11 严格 TypeScript 检查和 Vite 生产构建成功，生产构建转换 59 个模块并生成 4 个静态资源；`.\mvnw.cmd test` 成功，后端测试 1 个、失败 0、错误 0、跳过 0。
- `2026-07-17` 步骤 11 最终以前端锁定依赖恢复后的 `npm run build` 加后端模块 `package` 完成分段打包验证：4 个当前前端资源复制进后端并成功生成可执行 JAR，JAR 内容复查确认没有旧哈希资源。根聚合 `package` 本次因 Windows 残留 Node 进程占用 Rolldown 原生二进制，`npm ci` 无法替换该文件而未完成，不记录为成功。
- 自动化模型测试必须使用假 `ModelGateway`，不得消耗真实 API Key。
- `2026-07-17` 步骤 12 完整前端测试成功：15 个测试文件、97 项测试全部通过。新增 ProjectNavigation（6 项）、ProjectHeader（9 项）和 ProjectWorkspace（14 项）共 29 项组件测试。
- ProjectNavigation 测试覆盖全部 6 个导航项渲染、唯一当前项标记、点击事件发射和模块标签。
- ProjectHeader 测试覆盖项目名称、完整度百分比、六阶段中文标签、模型名称显示/隐藏、保存状态显示/隐藏、"生成 PRD"按钮和事件发射。
- ProjectWorkspace 测试覆盖加载中状态、项目未找到错误、数据库读取失败、工作台布局渲染、6 模块切换不离工作台、项目 ID 保持、右侧面板展开/收起切换、流程图和 PRD 页面默认面板收起、非流程图/PRD 页面默认展开、header 事件冒泡和辅助面板占位内容。
- `2026-07-17` 步骤 12 严格 TypeScript 检查和 Vite 生产构建成功，生产构建转换 68 个模块并生成 4 个静态资源；`.\mvnw.cmd test` 成功，后端测试 1 个、失败 0、错误 0、跳过 0。
- `2026-07-17` 步骤 12 `.\mvnw.cmd package` 成功：`npm ci` 安装 357 个锁定依赖，前端严格类型检查与生产构建、后端 1 个测试、4 个静态资源复制及 Spring Boot 可执行 JAR 重打包全部通过。
- 步骤 11 根聚合 `package` 因文件上传组件新增的 `@vueuse/core` 等依赖在 `mvnw.cmd` 隔离安装时触发 Windows 残留 Node 进程占用 Rolldown 原生二进制而未能完成；与步骤 11 相同的错误在步骤 12 后端模块独立 `package` 中不再出现，4 个当前哈希前端资源已正确进入 JAR。
- `2026-07-17` 步骤 13 完整前端测试成功：17 个测试文件、123 项测试全部通过。新增 RequirementRepository（15 项）和 VersionRepository（11 项）共 26 项测试。
- RequirementRepository 测试覆盖：按 ID 读取存在与不存在项、按项目列表（多项目过滤、空项目回空）、单条无版本更新（内容/标题/时间变化、连续 5 次调用不生成版本、缺失项抛错）、事务性保存（写入需求/版本/快照/变更、第二次保存替换全部旧需求、快照捕获问题/答案/冲突）、事务回滚（注入写入失败后无部分数据残留、原需求保持完整）、关闭重开后快照可恢复且版本差异可读，以及空白摘要拒绝、跨项目 ID 拒绝和缺失项目拒绝。
- VersionRepository 测试覆盖：按项目倒序列出版本、空项目回空列表、按 ID 读取存在与不存在版本、读取版本及关联变更、恢复到目标版本（保存当前状态、恢复需求数据、所有版本记录完整）、恢复后关闭重开状态保持、恢复不存在版本抛错且当前状态不变、跨项目恢复拒绝、恢复包含问题/答案/冲突的完整快照。
- `2026-07-17` 步骤 13 严格 TypeScript 检查和 Vite 生产构建成功；`.\mvnw.cmd test` 成功，后端测试 1 个、失败 0、错误 0、跳过 0。
- `2026-07-17` 步骤 13 `.\mvnw.cmd package` 跳过：与步骤 11-12 相同的 Windows Rolldown 原生二进制文件占用导致 `npm ci` 无法替换文件；前端测试与独立构建均验证通过，不重复记录。

- `2026-07-17` 步骤 14 完成统一 API 错误协议：创建 `ErrorCode` 枚举（8 种错误类别）、`ApiErrorResponse` record（code/message/requestId/timestamp）、`ApiException` 基类与 8 个子类、`RequestIdWebFilter`（UUID 注入响应头与 Reactor Context）、`GlobalExceptionHandler`（统一 JSON 响应、敏感信息脱敏）。
- `2026-07-17` 步骤 14 WebTestClient 测试覆盖全部 8 个错误类别（参数错误 400、鉴权 502、模型不存在 404、格式不兼容 422、限流 429、服务不可达 502、超时 504、内部错误 500），以及请求 ID 唯一性、时间戳、敏感信息扫描和 fallback 异常不泄露原始消息。
- `2026-07-17` 步骤 14 完成后运行 `.\mvnw.cmd test` 成功：后端测试 14 个、失败 0、错误 0、跳过 0；`npm --prefix frontend run test` 成功：17 个测试文件、123 项测试全部通过。
- `2026-07-17` 步骤 14 后端模块独立 `package` 成功；根聚合 `package` 因已知 Windows Rolldown 原生二进制文件占用跳过，与步骤 11-13 原因相同。

## 下一步

执行 `memory-bank/implementation-plan.md` 步骤 15，定义模型网关边界。
